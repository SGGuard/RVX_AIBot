# 📦 Свежие Дропы и Активности - Фича v0.15.0

Полностью реализована и развернута система для отслеживания актуальной информации о NFT дропах, новых токенах и активностях в Web3 экосистеме.

## 🎯 Что реализовано

### 1️⃣ Модуль Drops Tracker (`drops_tracker.py`)

Основной модуль для работы с данными о дропах и активностях.

**Основные функции:**

- `get_trending_tokens(limit=10)` - Получить вирусные токены за 24ч (данные CoinGecko)
- `get_nft_drops(limit=10)` - Получить актуальные NFT дропы по всем цепям
- `get_activities()` - Получить активности: стейкинг, ланчи, гавернанс, партнерства
- `get_drops_by_chain(chain)` - Дропы по конкретной цепи (arbitrum, solana, polygon, ethereum)
- `get_token_info(token_id)` - Подробная информация о токене
- `clear_cache()` - Очистка кэша вручную
- `get_cache_info()` - Информация о состоянии кэша

**Возможности:**
- ✅ Встроенное кэширование на 1 час
- ✅ Асинхронные запросы к CoinGecko API
- ✅ Обработка ошибок и fallback

### 2️⃣ API Endpoints (`api_server.py`)

Новые endpoints для получения данных о дропах:

#### `GET /get_drops?limit=10&chain=all`
Получить список NFT дропов.

**Параметры:**
- `limit` (int): Количество дропов (по умолчанию 10)
- `chain` (str): Цепь (arbitrum, solana, polygon, ethereum, all)

**Ответ:**
```json
{
  "drops": [
    {
      "name": "Magic Eden Launchpad",
      "symbol": "ME",
      "chain": "Solana",
      "price": "0.5 SOL",
      "time_until": "2h 30m",
      "status": "upcoming",
      "url": "https://magiceden.io"
    }
  ],
  "count": 5,
  "source": "CoinGecko + Launchpads",
  "timestamp": "2025-12-01T00:47:59.000797",
  "cache_ttl_minutes": 60
}
```

#### `GET /get_activities`
Получить активности в топ-проектах.

**Ответ:**
```json
{
  "staking_updates": [
    {
      "project": "Lido",
      "symbol": "LDO",
      "activity": "APY обновлен до 3.5%",
      "time": "2 часа назад",
      "chain": "Ethereum"
    }
  ],
  "new_launches": [
    {
      "project": "Arbitrum",
      "symbol": "ARB",
      "change": "+15.5%",
      "volume": "$50.2M",
      "time": "Последние 24ч",
      "chain": "Arbitrum"
    }
  ],
  "contract_updates": [],
  "governance": [
    {
      "project": "Uniswap",
      "symbol": "UNI",
      "proposal": "Prop #1000: Увеличить льготы коммьюнити",
      "time": "3 часа назад",
      "votes": "25K"
    }
  ],
  "partnerships": [
    {
      "project": "Arbitrum",
      "symbol": "ARB",
      "partnership": "Партнерство с Chainlink",
      "time": "Вчера",
      "impact": "Интеграция в экосистему"
    }
  ],
  "total_activities": 5,
  "timestamp": "2025-12-01T00:48:03.690138",
  "cache_ttl_minutes": 60
}
```

#### `GET /get_trending?limit=10`
Получить трендовые токены.

**Ответ:**
```json
{
  "drops": [
    {
      "symbol": "MON",
      "name": "Monad",
      "market_cap_rank": 211,
      "thumb": "https://...",
      "score": 0,
      "type": "token",
      "chain": "Multi",
      "timestamp": "2025-12-01T00:48:08.429337"
    }
  ],
  "count": 10,
  "source": "CoinGecko Trending API",
  "timestamp": "2025-12-01T00:48:08.429337",
  "cache_ttl_minutes": 60
}
```

#### `GET /get_token_info/{token_id}`
Получить подробную информацию о конкретном токене.

**Пример:** `GET /get_token_info/bitcoin`

**Ответ:**
```json
{
  "name": "Bitcoin",
  "symbol": "BTC",
  "price": 98750.5,
  "market_cap": 1950000000000,
  "market_cap_rank": 1,
  "change_24h": 2.5,
  "change_7d": 8.3,
  "volume_24h": 45000000000,
  "ath": 108000,
  "atl": 100,
  "timestamp": "2025-12-01T00:48:10.123456"
}
```

### 3️⃣ Telegram Bot Команды (`bot.py`)

Новые команды для пользователей:

#### `/drops` - 📦 Свежие NFT дропы
Показывает топ-10 актуальных NFT дропов со всех цепей.

**Что показывает:**
- Название коллекции
- Цена в нативной валюте
- Время запуска
- Цепь (Arbitrum, Solana, Polygon, Ethereum)
- Ссылка на проект

#### `/activities` - 🔥 Активности в проектах
Показывает актуальные активности в топ-проектах:

- **Стейкинг** - обновления APY
- **Новые ланчи** - события и рост токенов
- **Гавернанс** - активные предложения
- **Партнерства** - новые сотрудничества

#### `/trending` - 📈 Вирусные токены
Показывает токены, набирающие популярность последние 24 часа.

**Информация:**
- Рыночный рейтинг
- Название и символ
- Тренд-скор

#### `/subscribe_drops` - 📢 Подписка на уведомления
Интерактивное меню для подписки на уведомления о дропах по цепям:

- Arbitrum 🔷
- Solana ◎
- Polygon 🟣
- Ethereum 🔹
- Все цепи 🌐
- Отписаться ❌

#### `/my_subscriptions` - 📋 Мои подписки
Показывает текущие активные подписки.

### 4️⃣ База данных

Добавлены 3 новые таблицы:

#### `user_drop_subscriptions`
```sql
CREATE TABLE user_drop_subscriptions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,
    chain TEXT,
    notify_interval TEXT DEFAULT 'daily',
    enabled BOOLEAN DEFAULT 1,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### `drops_history`
```sql
CREATE TABLE drops_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,
    drop_name TEXT,
    drop_type TEXT,
    chain TEXT,
    viewed_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    is_new BOOLEAN DEFAULT 1
);
```

#### `activities_cache`
```sql
CREATE TABLE activities_cache (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    activity_type TEXT,
    project_name TEXT,
    activity_data TEXT,
    chain TEXT,
    cached_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    expires_at TIMESTAMP
);
```

## 📊 Примеры использования

### Curl

```bash
# Получить топ-5 дропов
curl -X GET "http://localhost:8000/get_drops?limit=5"

# Получить дропы на Arbitrum
curl -X GET "http://localhost:8000/get_drops?chain=arbitrum"

# Получить текущие активности
curl -X GET "http://localhost:8000/get_activities"

# Получить трендовые токены
curl -X GET "http://localhost:8000/get_trending?limit=15"

# Получить информацию о Bitcoin
curl -X GET "http://localhost:8000/get_token_info/bitcoin"
```

### Python

```python
import asyncio
from drops_tracker import (
    get_trending_tokens, get_nft_drops, get_activities,
    get_drops_by_chain, get_token_info
)

async def main():
    # Получить вирусные токены
    trending = await get_trending_tokens(limit=10)
    print(f"Трендовые токены: {trending}")
    
    # Получить NFT дропы
    drops = await get_nft_drops(limit=5)
    print(f"NFT дропы: {drops}")
    
    # Получить активности
    activities = await get_activities()
    print(f"Активности: {activities}")
    
    # Получить дропы на Polygon
    polygon_drops = await get_drops_by_chain("polygon")
    print(f"Polygon дропы: {polygon_drops}")
    
    # Получить информацию о токене
    eth_info = await get_token_info("ethereum")
    print(f"Ethereum: {eth_info}")

asyncio.run(main())
```

## 🔄 Интеграция с лимитами

Все команды учитывают дневные лимиты запросов пользователя:

```python
# Проверка лимита перед использованием команды
can_proceed, limit_info = check_daily_limit(user_id)
if not can_proceed:
    await update.message.reply_text(f"⚠️ Лимит исчерпан: {limit_info}")
    return

# Увеличение счетчика запросов
increment_daily_requests(user_id)
```

## 🔐 Безопасность и Performance

- ✅ **Кэширование**: 1 час для всех данных
- ✅ **Асинхронность**: Все API запросы асинхронные
- ✅ **Таймауты**: 10-15 секунд для всех запросов
- ✅ **Обработка ошибок**: Graceful fallback при ошибках
- ✅ **Логирование**: Полное логирование всех операций
- ✅ **Лимиты**: Интеграция с XP-зависимыми лимитами

## 📈 Версия и изменения

**v0.15.0 - Drops & Activities Tracker**

- ✅ Создан модуль `drops_tracker.py` с CoinGecko интеграцией
- ✅ Добавлены 4 API endpoints для получения данных
- ✅ Добавлены 5 Telegram команд
- ✅ Добавлены 3 таблицы в БД для хранения подписок и истории
- ✅ Полная интеграция с существующей системой лимитов
- ✅ Встроенное кэширование и обработка ошибок

## 🚀 Будущие улучшения

**v0.16.0 (планируется):**
- Background job для отправки уведомлений о новых дропах каждый час
- Система оценок дропов (risk, popularity, APY expected)
- Интеграция с DeFi протоколами для информации о стейкинге
- Сравнение цен дропов между цепями
- Аналитика: какие дропы популярны среди пользователей

## 📞 Поддержка

Если есть вопросы или ошибки:
1. Проверь логи: `tail -f bot.log` или `tail -f api_server.log`
2. Проверь, что API сервер доступен: `curl http://localhost:8000/health`
3. Проверь, что у тебя не исчерпан дневной лимит: `/limits`

---

**Версия:** 0.15.0  
**Дата:** 1 декабря 2025  
**Статус:** ✅ Полностью готова к использованию
