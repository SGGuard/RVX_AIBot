# 🚀 Railway Deployment Guide

## 📋 Краткое резюме

Этот гайд поможет вам развернуть RVX AI Crypto News Bot на Railway.app в staging окружении.

**Требования:**
- GitHub аккаунт с доступом к репозиторию SGGuard/RVX_AIBot
- Railway.app аккаунт (бесплатный)
- Все API ключи переновлены (после очистки Git истории)

**Время развертывания:** ~5-10 минут

---

## 🔐 БЕЗОПАСНОСТЬ (КРИТИЧНО!)

### ✅ Проверьте перед началом:

1. **Git история чиста от ключей:**
   ```bash
   git log --all -p | grep -i "gsk_\|AIzaSy\|sk-test\|sk-live"
   # Не должно быть результатов!
   ```

2. **Нет ключей в текущих файлах:**
   - `.env` — НИКОГДА не коммитьте (защищен `.gitignore`) ✅
   - `.env.example` — только плейсхолдеры ✅
   - Остальной код — без ключей ✅

3. **Все ключи переновлены:**
   - [ ] Telegram Bot Token (@BotFather)
   - [ ] Groq API Key (groq.com/keys)
   - [ ] Mistral API Key (mistral.ai)
   - [ ] DeepSeek API Key (deepseek.com)
   - [ ] Gemini API Key (makersuite.google.com)

---

## 🚀 ШАГИ РАЗВЕРТЫВАНИЯ

### Шаг 1: Подготовка файлов (УЖЕ ГОТОВО ✅)

Все необходимые файлы уже в репозитории:
- ✅ `Procfile` — конфигурация процессов для Railway
- ✅ `railway.json` — параметры Railway
- ✅ `Dockerfile` — контейнеризация
- ✅ `requirements.txt` — зависимости Python
- ✅ `.env.example` — шаблон переменных (БЕЗ ключей)

### Шаг 2: Откройте Railway.app и создайте проект

1. Перейдите на **https://railway.app/new**
2. Выберите **"Deploy from GitHub"**
3. Авторизуйтесь GitHub если нужно
4. Найдите репозиторий **SGGuard/RVX_AIBot**
5. Нажмите **"Deploy Now"**

Railway автоматически:
- Клонирует репозиторий
- Найдет `Procfile`
- Найдет `requirements.txt`
- Начнет сборку (2-3 минуты)

### Шаг 3: Добавьте переменные окружения

⚠️ **КРИТИЧНО:** Переменные добавляются ТОЛЬКО в Railway UI, не в коде!

1. В Railway Dashboard → ваш проект
2. Перейдите на вкладку **"Variables"**
3. Нажмите **"Add Variable"** и добавьте:

```
TELEGRAM_BOT_TOKEN          = <ваш_реальный_токен_от_@BotFather>
GROQ_API_KEY                = <ваш_реальный_ключ_groq>
MISTRAL_API_KEY             = <ваш_реальный_ключ_mistral>
DEEPSEEK_API_KEY            = <ваш_реальный_ключ_deepseek>
GEMINI_API_KEY              = <ваш_реальный_ключ_gemini>
GEMINI_MODEL                = models/gemini-2.5-flash
GEMINI_TEMPERATURE          = 0.3
GEMINI_MAX_TOKENS           = 1500
GEMINI_TIMEOUT              = 30
PORT                        = 8000
MAX_TEXT_LENGTH             = 4096
CACHE_ENABLED               = true
ALLOWED_ORIGINS             = *
LOG_LEVEL                   = INFO
MAX_REQUESTS_PER_MINUTE     = 30
CACHE_MAX_SIZE              = 100
CACHE_TTL                   = 3600
```

4. Нажмите **"Save"** — Railway автоматически перезагрузит приложение

### Шаг 4: Настройка Telegram Webhook

⚠️ **ВАЖНО:** Боту нужен URL для получения сообщений от Telegram

1. В Railway Dashboard → ваш проект → **"Deployments"**
2. Скопируйте **Domain URL** (выглядит как `https://rvx-bot-production.up.railway.app`)

3. Установите Webhook для Telegram:
```bash
# Замените YOUR_TOKEN и YOUR_URL
curl -X POST https://api.telegram.org/botYOUR_TOKEN/setWebhook \
  -d "url=https://YOUR_URL/webhook"

# Пример:
curl -X POST https://api.telegram.org/bot1234567890:ABCDefghijklmnop/setWebhook \
  -d "url=https://rvx-bot-production.up.railway.app/webhook"
```

4. Проверьте установку:
```bash
curl https://api.telegram.org/botYOUR_TOKEN/getWebhookInfo
```

Должно вернуть:
```json
{
  "ok": true,
  "result": {
    "url": "https://rvx-bot-production.up.railway.app/webhook",
    "has_custom_certificate": false,
    "pending_update_count": 0
  }
}
```

### Шаг 5: Проверка развертывания

#### 5.1 Проверьте логи
1. Railway Dashboard → ваш проект → **"Logs"**
2. Ищите:
   ```
   INFO: Started server process
   INFO: Uvicorn running on 0.0.0.0:8000
   ```

#### 5.2 Проверьте здоровье API
```bash
curl https://YOUR_RAILWAY_URL/health
```

Должно вернуть:
```json
{
  "status": "ok",
  "timestamp": "2025-12-10T10:30:00Z"
}
```

#### 5.3 Протестируйте Telegram бота
1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Должны получить приветственное сообщение

---

## 🐛 Решение проблем

### ❌ "Build failed"
**Решение:**
1. Проверьте логи: Railway Dashboard → Logs
2. Убедитесь что `requirements.txt` актуален
3. Проверьте что `Procfile` существует и правильный

### ❌ "Webhook not working"
**Решение:**
1. Проверьте что TELEGRAM_BOT_TOKEN добавлен в Variables
2. Переустановите webhook:
   ```bash
   curl -X POST https://api.telegram.org/botTOKEN/setWebhook -d "url=YOUR_URL/webhook"
   ```
3. Проверьте логи бота

### ❌ "API Key validation error"
**Решение:**
- Убедитесь что ключи скопированы БЕЗ пробелов в начале/конце
- Проверьте что ключи действительны на сервисах (не истекли)
- Попробуйте обновить ключ на сервисе и добавить снова

### ❌ "Database lock error"
**Решение:**
- SQLite в Railway может быть медленной
- Для production рассмотрите PostgreSQL (Railway предоставляет бесплатно)
- Пока: перезагрузите приложение (Railway → Deployments → Redeploy)

---

## 📊 Мониторинг

### Проверяйте регулярно:

**Логи приложения:**
- Railway Dashboard → Logs
- Ищите ERROR или WARNING

**Использование ресурсов:**
- Railway Dashboard → Metrics
- CPU, Memory, Network должны быть OK

**Telegram логирование:**
- Проверьте что боту приходят сообщения
- Тестовое сообщение должно обработаться за <10 сек

---

## 🔄 Обновление кода

### Когда обновили код на GitHub:

**Автоматическое развертывание** (если включено):
1. Railway автоматически перестраивает образ
2. Заново запускает приложение
3. Примерно 2-3 минуты на сборку

**Ручное развертывание:**
1. Railway Dashboard → Deployments
2. Нажмите "Redeploy"

### Обновление переменных окружения:

1. Railway Dashboard → Variables
2. Отредактируйте значение
3. Нажмите "Save"
4. Railway автоматически перезагружает приложение

---

## 📈 Следующие шаги

После успешного развертывания staging:

1. **Проведите тестирование** (24-48 часов)
   - Проверьте все функции бота
   - Проверьте анализ новостей
   - Проверьте обработку ошибок

2. **Мониторьте логи и метрики**
   - Ищите ошибки
   - Проверяйте время ответа
   - Следите за использованием ресурсов

3. **Готовьтесь к production:**
   - Если staging OK → можно готовить production
   - Рассмотрите PostgreSQL вместо SQLite
   - Настройте бэкапы БД

---

## 💡 Советы

1. **Храните информацию о Railway URL:**
   - Может пригодиться для webhook'ов
   - Используйте для тестирования API

2. **Не забывайте про переновление ключей:**
   - Если заподозрили компромисс
   - Регулярно (раз в месяц) на production

3. **Используйте разные ключи для staging и production:**
   - Так вы ограничите последствия если staging будет взломан

4. **Версионируйте релизы:**
   ```bash
   git tag v0.18.0-staging
   git push --tags
   ```

---

## ✨ Успешное развертывание!

Поздравляем! Ваш бот теперь на Railway! 🎉

**Что дальше:**
- Тестируйте функциональность
- Мониторьте логи
- Готовьтесь к production (если все OK)
- Продолжайте разработку

---

**Документация:**
- [Railway Docs](https://docs.railway.app/)
- [Python-Telegram-Bot Docs](https://python-telegram-bot.org/)
- [FastAPI Docs](https://fastapi.tiangolo.com/)

**Поддержка:**
Если есть вопросы - проверьте:
1. Логи Railway
2. `RAILWAY_DEPLOYMENT_CHECKLIST.md`
3. Документацию сервисов
