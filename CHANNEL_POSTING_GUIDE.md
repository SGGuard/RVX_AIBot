# 📢 Гайд: Публикация постов в канал

## 🎯 Что это?

Система автоматической публикации постов об обновлениях бота в Telegram канал. Бот может писать в канал при обновлениях, новых квестах, техническом обслуживании и достижении вех.

## ⚙️ Настройка

### 1. Добавьте переменную в `.env`:

```bash
# Канал для публикации постов об обновлениях (тот где бот админ)
UPDATE_CHANNEL_ID=-1001234567890  # Получить ID можно через @userinfobot в канале
```

**Как получить ID канала:**
1. Откройте канал в Telegram
2. Используйте бота [@userinfobot](https://t.me/userinfobot) - отправьте /start
3. Перешлите сообщение из канала боту
4. Скопируйте ID в формате `-100XXXXX`

### 2. Убедитесь, что бот админ в канале

Бот должен иметь права на отправку сообщений в канал.

## 📝 Доступные функции

### 1. **notify_version_update** - Обновление версии

```python
# Использование в коде:
await notify_version_update(context, "0.15.0", """• Новая система квестов v2
• Улучшена производительность API
• Добавлены новые бейджи""")
```

**Пример поста:**
```
🚀 ОБНОВЛЕНИЕ БОТА - Версия 0.15.0

Что нового:
• Новая система квестов v2
• Улучшена производительность API
• Добавлены новые бейджи

⏰ Обновление: 30.11.2025 14:30:45

✨ Спасибо за активность! Ваши отзывы помогают улучшить бот!
```

---

### 2. **notify_new_quests** - Новые ежедневные квесты

```python
# Использование:
await notify_new_quests(context)
```

**Пример поста:**
```
📋 Новые ежедневные квесты готовы!

✅ У вас доступно 5 новых квестов

🎁 Полученные награды:
• XP за выполнение
• Бейджи за достижения
• Увеличение лимита запросов

💡 Совет: Используйте команду /tasks для просмотра квестов

⚡ Лимит запросов зависит от вашего уровня!
```

---

### 3. **notify_system_maintenance** - Техническое обслуживание

```python
# Использование:
await notify_system_maintenance(context, duration_minutes=10)
```

**Пример поста:**
```
🔧 Техническое обслуживание

⏸️ Бот временно недоступен для обслуживания

⏱️ Ожидаемая длительность: ~10 минут

🔄 Система будет восстановлена вскоре

Спасибо за ваше терпение!
```

---

### 4. **notify_milestone_reached** - Веха достигнута

```python
# Использование:
await notify_milestone_reached(context, "100 активных пользователей", 100)
```

**Пример поста:**
```
🎉 Веха достигнута: 100 активных пользователей!

📈 В сообществе RVX 100 активных пользователей!

🙏 Спасибо за вашу поддержку и активность

✨ Продолжайте учиться и развиваться вместе с нами!
```

---

### 5. **notify_new_feature** - Новая функция

```python
# Использование:
await notify_new_feature(
    context, 
    "Рейтинг полезности ответов",
    "Теперь вы можете оценивать полезность ответов бота через 👍👎 кнопки!"
)
```

**Пример поста:**
```
✨ Новая функция: Рейтинг полезности ответов

📝 Теперь вы можете оценивать полезность ответов бота через 👍👎 кнопки!

🎯 Используйте /help для подробной информации

💪 Продолжайте развиваться с новыми возможностями!
```

---

### 6. **notify_stats_milestone** - Рекорд статистики

```python
# Использование:
await notify_stats_milestone(context, "Максимум запросов в день", "5000+ запросов обработано!")
```

**Пример поста:**
```
📊 Новый рекорд: Максимум запросов в день

🏆 5000+ запросов обработано!

🔥 Это показывает активность нашего сообщества!

✨ Спасибо вам всем за участие!
```

---

### 7. **send_channel_post** - Произвольный пост

```python
# Использование для кастомных постов:
await send_channel_post(
    context,
    "<b>Кастомный пост!</b>\n\nЭто может быть любой текст с HTML форматированием",
    parse_mode=ParseMode.HTML,
    silent=True  # True = без звука, False = с звуком
)
```

## 🔧 Как интегрировать в команды

### Пример: Уведомление при перезапуске

```python
async def restart_command(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Команда для перезапуска бота (только для админов)."""
    user_id = update.effective_user.id
    
    if user_id not in ADMIN_USERS:
        await update.message.reply_text("❌ Только админы могут перезапустить бота")
        return
    
    # Уведомляем в канал
    await notify_system_maintenance(context, duration_minutes=2)
    
    # Перезапускаем
    logger.info("🔄 Перезапуск бота...")
    os.system("pkill -f 'python.*bot.py'")
```

### Пример: Уведомление при деплое

Добавьте в `main()` при запуске:

```python
async def send_startup_notification(context: ContextTypes.DEFAULT_TYPE):
    """Отправляет уведомление о запуске в канал."""
    await notify_version_update(
        context,
        BOT_VERSION,
        "✅ Бот перезапущен и готов к работе\n• Все системы в норме\n• API доступен"
    )

# В main() добавьте:
job_queue.run_once(send_startup_notification, when=2)  # Через 2 секунды
```

## 📊 Примеры использования в реальных сценариях

### Сценарий 1: Автоматическое уведомление о новых квестах (каждый день)

```python
async def notify_daily_quests_task(context: ContextTypes.DEFAULT_TYPE):
    """Ежедневный таск отправки уведомления о квестах."""
    await notify_new_quests(context)

# В main():
job_queue.run_daily(
    notify_daily_quests_task,
    time=datetime.time(hour=9, minute=0),  # 9:00 каждый день
    days=(0, 1, 2, 3, 4, 5, 6)  # Все дни
)
```

### Сценарий 2: Уведомление о вехах (проверка каждый час)

```python
async def check_milestone_task(context: ContextTypes.DEFAULT_TYPE):
    """Проверяет достижение вех и отправляет уведомления."""
    with get_db() as conn:
        cursor = conn.cursor()
        cursor.execute("SELECT COUNT(*) FROM users WHERE created_at IS NOT NULL")
        user_count = cursor.fetchone()[0]
        
        # Проверяем известные вехи
        milestones = [50, 100, 200, 500, 1000]
        for milestone in milestones:
            if user_count == milestone:
                await notify_milestone_reached(context, f"{milestone} активных пользователей", milestone)

# В main():
job_queue.run_repeating(
    check_milestone_task,
    interval=3600,  # Каждый час
    first=60  # Первый запуск через минуту
)
```

### Сценарий 3: Уведомление при ошибках API

```python
async def error_handler(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Глобальный обработчик ошибок - уведомляет в канал при критических ошибках."""
    if isinstance(context.error, TelegramError):
        await notify_system_maintenance(context, duration_minutes=5)
        logger.error(f"Критическая ошибка: {context.error}")
```

## ✅ Проверка работоспособности

```bash
# Проверьте что переменная установлена:
grep UPDATE_CHANNEL_ID .env

# Если переменной нет, добавьте:
echo "UPDATE_CHANNEL_ID=-100123456789" >> .env
```

## 🚀 Активация в боте

1. Обновите `.env`:
   ```
   UPDATE_CHANNEL_ID=-1001234567890
   ```

2. Перезагрузите бот:
   ```bash
   pkill -f "python.*bot.py"
   python3 bot.py &
   ```

3. Проверьте логи:
   ```bash
   tail -f bot.log | grep -E "(UPDATE_CHANNEL|📢|Пост отправлен)"
   ```

## 🎯 Рекомендуемые сценарии

| Сценарий | Функция | Частота |
|----------|---------|---------|
| Новая версия | `notify_version_update` | При деплое |
| Новые квесты | `notify_new_quests` | Ежедневно 09:00 |
| Техническое обслуживание | `notify_system_maintenance` | По мере необходимости |
| Новая функция | `notify_new_feature` | При выпуске фичи |
| Рекорды статистики | `notify_stats_milestone` | При достижении |
| Вехи пользователей | `notify_milestone_reached` | При 100, 500, 1000 юзеров |

## 🔐 Безопасность

- ✅ Функции проверяют наличие UPDATE_CHANNEL_ID перед отправкой
- ✅ Все ошибки логируются
- ✅ Отправка не блокирует работу бота (асинхронная)
- ✅ Поддерживает HTML форматирование для красивых постов

## 🤝 Примечания

- Если канал не указан, система логирует warning и продолжает работу
- Посты отправляются **без звука** по умолчанию (можно изменить `silent=False`)
- Все временные метки в формате UTC (можно изменить в `datetime.now()`)
- Поддерживает HTML и Markdown форматирование

---

**Готово! Теперь бот может писать посты в канал при каждом обновлении! 🚀**
