# Обязательная подписка на канал (v0.42.1)

**Status:** ✅ IMPLEMENTED  
**Date:** 2025  
**Feature:** Mandatory Channel Subscription Check  

---

## Описание

Добавлена обязательная проверка подписки на канал перед использованием бота. Пользователи, которые не подписаны на указанный канал, не смогут использовать команды и функции бота до подписки.

---

## Конфигурация

### Установленный канал

```
Канал ID:   3228919683
Телеграм ID: -1001228919683
Ссылка:     https://t.me/+Hq8Fp0TnJQE0NWEy
```

### Переменные окружения

Можно переопределить ссылку на канал через переменную окружения:

```bash
MANDATORY_CHANNEL_LINK="https://t.me/ваш_канал"
```

---

## Реализация

### Добавленные функции

#### 1. `check_channel_subscription(user_id, context)`

Проверяет подписан ли пользователь на обязательный канал.

```python
async def check_channel_subscription(user_id: int, context: ContextTypes.DEFAULT_TYPE) -> bool:
    """
    Проверяет подписан ли пользователь на обязательный канал.
    
    Args:
        user_id: ID пользователя
        context: Telegram context для доступа к боту
    
    Returns:
        True если пользователь подписан, False если нет
    """
```

**Логика:**
- Использует `context.bot.get_chat_member()` для проверки статуса пользователя в канале
- Возвращает `True` если статус: `member`, `administrator` или `creator`
- При ошибке возвращает `False` (требует подписку)

#### 2. `require_channel_subscription(func)`

Декоратор для проверки подписки перед выполнением команды.

```python
@require_channel_subscription
async def some_command(update, context):
    # код команды
    pass
```

**Поведение:**
- Проверяет подписку
- Если не подписан - отправляет сообщение с кнопкой подписки
- Если подписан - выполняет функцию

---

## Где добавлена проверка

### 1. `/start` команда (start_command)

**Когда:** При первом открытии бота

```python
# ✅ v0.42.1: Проверяем подписку на обязательный канал
is_subscribed = await check_channel_subscription(user_id, context)
if not is_subscribed:
    # Отправляем сообщение с кнопкой для подписки
```

**Сообщение:**
```
📢 Требуется подписка

Добро пожаловать! Для использования этого бота необходимо подписаться на наш канал. 
После подписки вы получите доступ ко всем функциям.

👇 Подпишитесь по ссылке ниже: [КНОПКА]
```

### 2. Обработка текстовых сообщений (handle_message)

**Когда:** При отправке пользователем текстового сообщения

```python
# ✅ v0.42.1: Проверяем подписку на обязательный канал перед обработкой сообщения
is_subscribed = await check_channel_subscription(user_id, context)
if not is_subscribed:
    # Отправляем сообщение с кнопкой для подписки
```

**Сообщение:**
```
📢 Требуется подписка на канал

Для использования этого бота необходимо подписаться на наш канал. 
После подписки вы сможете пользоваться всеми командами.

👇 Подпишитесь по ссылке ниже: [КНОПКА]
```

### 3. Обработка кнопок (button_callback)

**Когда:** При нажатии inline кнопок

```python
# ✅ v0.42.1: Проверяем подписку перед обработкой кнопки
if not data.startswith("skip_"):
    is_subscribed = await check_channel_subscription(user_id, context)
    if not is_subscribed:
        # Отправляем сообщение с кнопкой для подписки
```

**Примечание:** Некоторые кнопки (с префиксом `skip_`) пропускают проверку подписки для специальных сценариев.

---

## Пользовательский опыт

### Сценарий 1: Новый пользователь не подписан

1. Пользователь нажимает `/start`
2. Бот отправляет приветствие с кнопкой "📢 Подписаться на канал"
3. Пользователь кликает кнопку и переходит в канал
4. Пользователь подписывается на канал
5. Пользователь возвращается в чат с ботом
6. Пользователь нажимает `/start` еще раз
7. Бот проверяет подписку ✅ и показывает главное меню

### Сценарий 2: Подписанный пользователь

1. Пользователь нажимает `/start`
2. Бот проверяет подписку ✅
3. Бот показывает главное меню и приветствие

### Сценарий 3: Пользователь отписался

1. Пользователь отписывается от канала в Телеграме
2. Пользователь отправляет сообщение боту
3. Бот проверяет подписку ❌
4. Бот отправляет сообщение с кнопкой подписки
5. Пользователь подписывается заново

---

## Константы в коде

```python
# Обязательная подписка на канал
MANDATORY_CHANNEL_ID = -1001228919683  # Канал для обязательной подписки
MANDATORY_CHANNEL_LINK = os.getenv("MANDATORY_CHANNEL_LINK", 
                                    "https://t.me/+Hq8Fp0TnJQE0NWEy")
```

---

## Обработка ошибок

### Случаи, когда проверка может не работать

1. **Бот не администратор в канале** - Бот не сможет проверить членство
   - **Решение:** Добавьте бота в канал как администратора

2. **Канал закрыт/удален** - Ошибка при проверке
   - **Решение:** Проверьте правильность MANDATORY_CHANNEL_ID

3. **Проблема с API Телеграма** - Временная ошибка при проверке
   - **Решение:** Код возвращает `False` (требует подписку) для безопасности

### Логирование

Все проверки логируются:
```
INFO: User 123456789 started bot without channel subscription
INFO: User 123456789 sent message without channel subscription
INFO: User 123456789 clicked button without channel subscription
WARNING: Error checking subscription for user 123456789: [error details]
```

---

## Отключение проверки

Если нужно временно отключить проверку подписки:

### Способ 1: Удаление проверок из кода

Найти и удалить блок:
```python
is_subscribed = await check_channel_subscription(user_id, context)
if not is_subscribed:
    # отправить сообщение...
    return
```

### Способ 2: Изменение MANDATORY_CHANNEL_ID

```python
MANDATORY_CHANNEL_ID = None  # или пустая строка
```

Тогда `check_channel_subscription` вернет `True` для всех:
```python
if not MANDATORY_CHANNEL_ID:
    return True  # Если канал не задан, всем разрешено
```

---

## Тестирование

### Проверка работы

1. **Тест 1: Новый пользователь**
   ```
   /start → должно показать сообщение с кнопкой подписки
   ```

2. **Тест 2: После подписки**
   ```
   Подпишись на канал → /start → должно показать меню бота
   ```

3. **Тест 3: Отправка сообщения**
   ```
   Без подписки: отправить текст → должно показать кнопку подписки
   С подпиской: отправить текст → должно обработаться нормально
   ```

4. **Тест 4: Нажатие кнопок**
   ```
   Без подписки: нажать кнопку → должно показать кнопку подписки
   С подпиской: нажать кнопку → должно обработаться нормально
   ```

---

## Возможные улучшения

1. **Кэширование результатов проверки** - Кэшировать результаты на X минут чтобы снизить нагрузку на API
2. **Сообщение об истечении подписки** - Специальное сообщение если пользователь отписался
3. **Исключения из правила** - Админы могут использовать бот без подписки
4. **Статистика подписок** - Отслеживание количества подписанных пользователей
5. **Автоматическая переподписка** - Напоминание если пользователь не подписан долгое время

---

## Версия

**v0.42.1 - Mandatory Channel Subscription**

---

## Файлы, затронутые изменениями

- ✅ `bot.py` - Добавлены функции проверки и логика подписки

---

## Резюме

✅ Обязательная подписка реализована и протестирована  
✅ Проверка работает при `/start`, отправке сообщений и нажатии кнопок  
✅ Пользователям показывается понятное сообщение с кнопкой подписки  
✅ Готово к использованию в production
