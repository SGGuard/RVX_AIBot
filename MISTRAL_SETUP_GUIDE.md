# 🔐 MISTRAL API - БЕСПЛАТНАЯ НАДЕЖНОСТЬ

**Статус:** ✅ Опциональное добавление для v0.24  
**Дата:** 8 декабря 2025  
**Стоимость:** БЕСПЛАТНО (2М токенов/месяц без карточки)

---

## 🎯 ЗАЧЕМ MISTRAL?

### Текущее состояние (v0.24)
```
Запрос → Groq ✅ → Gemini ✅ → Fallback ⚠️
```

### После добавления Mistral
```
Запрос → Groq ✅ → Mistral ✅ → Gemini ✅ → Fallback ✅
```

**Результат:** 3 независимых провайдера = гарантированный ответ!

---

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ

### Шаг 1: Получить Mistral API ключ (2 минуты)

1. Перейди на: https://console.mistral.ai/
2. Нажми "Sign up" или зарегистрируйся через:
   - Email + пароль
   - Google аккаунт  
   - GitHub аккаунт

3. После входа найди в меню:
   - Слева: "API keys" или "Keys"
   
4. Нажми "+ Create new API key" или "Create new key"

5. Дай имя: "RVX Bot" (любое имя)

6. Скопируй появившийся ключ:
   ```
   sk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```

**⏱️ Время:** 2 минуты  
**💰 Стоимость:** БЕСПЛАТНО  
**📋 Требования:** Email или соцсеть

---

### Шаг 2: Добавить ключ в систему (30 секунд)

**Способ 1: Автоматический (рекомендуется)**

```bash
bash /tmp/update_mistral_key.sh
```

Утилита попросит вставить ключ, сама обновит .env и создаст резервную копию.

**Способ 2: Вручную**

1. Открой файл `.env`:
   ```bash
   nano /home/sv4096/rvx_backend/.env
   ```

2. Найди строку:
   ```
   MISTRAL_API_KEY="ЗАМЕНИ_НА_КЛЮЧ_ИЗ_MISTRAL"
   ```

3. Замени на:
   ```
   MISTRAL_API_KEY="sk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
   ```

4. Сохрани (Ctrl+O, Enter, Ctrl+X)

---

### Шаг 3: Перезапустить сервисы (30 секунд)

**Способ 1: Автоматический скрипт (рекомендуется)**

```bash
bash /tmp/restart_services.sh
```

Скрипт:
- Остановит текущие сервисы
- Запустит новые
- Покажет статус
- Проверит логи
- Выведет метрики

**Способ 2: Вручную**

```bash
# Остановить текущие
pkill -f "python3.*api_server.py"
pkill -f "python3.*bot.py"
sleep 2

# Запустить новые
cd /home/sv4096/rvx_backend
python3 api_server.py > /tmp/api_server.log 2>&1 &
python3 bot.py > /tmp/bot.log 2>&1 &
```

---

## ✅ ПРОВЕРКА

### Проверить что Mistral загружен

```bash
curl http://localhost:8000/dialogue_metrics | grep mistral
```

Должно показать:
```json
"mistral": {
  "requests": 0,
  "success": 0,
  "errors": 0,
  "timeouts": 0
}
```

### Проверить логи

```bash
# Видеть когда используется Mistral
tail -f /tmp/api_server.log | grep -i mistral
```

При first failure Groq → Mistral будет:
```
🔄 Mistral: Получаем ответ (fallback 1)...
✅ Mistral OK (250 символов, 0.35s)
```

### Проверить полные метрики

```bash
curl http://localhost:8000/dialogue_metrics | python3 -m json.tool
```

---

## 💰 БЕСПЛАТНЫЙ ЛИМИТ

### Что Mistral дает бесплатно?

- ✅ **2,000,000 токенов/месяц** (без платежа!)
- ✅ **~10,000 запросов** среднего размера
- ✅ **~2,000 полных диалогов**
- ✅ **Без ввода карточки**
- ✅ **Без сроков действия**

### Расчет токенов

```
1 запрос = ~200-500 токенов
2,000,000 токенов / 350 (средний) = ~5,700 запросов/месяц

Или на русском:
Примерно 190 запросов в день, каждый день месяца!
```

### Что если лимит кончится?

```
Mistral упадет (HTTP 429)
    ↓
Система переключится на Gemini
    ↓
Ответ все равно придет!
```

---

## 🎯 АРХИТЕКТУРА ПОСЛЕ ДОБАВЛЕНИЯ

### Flow диалога

```
Пользователь: "Привет"
      ↓
    bot.py
      ↓
   handle_message()
      ↓
   ai_dialogue.get_ai_response_sync()
      ↓
   1️⃣ GROQ (быстро, 400ms)  ← ОСНОВНОЙ
      ↓ (если ошибка)
   2️⃣ MISTRAL (fast, 350ms)  ← FALLBACK 1 (НОВОЕ!)
      ↓ (если ошибка)
   3️⃣ GEMINI (медленно, 2s)  ← FALLBACK 2
      ↓ (если ошибка)
   Fallback ответ
      ↓
Ответ пользователю
```

### Надежность

| Сценарий | v0.23 | v0.24 |
|----------|-------|-------|
| Groq упал | Gemini (20/день) | Mistral (2M/месяц) |
| Groq + Gemini упали | Fallback | Mistral + Fallback |
| Все упали | ❌ Нет ответа | ✅ Fallback ответ |

---

## 📝 КОМАНДЫ СПРАВКИ

### Быстрые команды

```bash
# Добавить Mistral ключ (интерактивно)
bash /tmp/update_mistral_key.sh

# Перезапустить все сервисы
bash /tmp/restart_services.sh

# Проверить метрики
curl http://localhost:8000/dialogue_metrics

# Посмотреть только Mistral метрики
curl http://localhost:8000/dialogue_metrics | grep -A 5 mistral

# Смотреть логи в реальном времени
tail -f /tmp/bot.log

# Найти все упоминания Mistral в логах
grep -i mistral /tmp/api_server.log

# Проверить что оба сервиса запущены
ps aux | grep -E "python3.*(api_server|bot)" | grep -v grep
```

---

## ❓ FAQ

### Q: Mistral действительно бесплатный?
**A:** ДА! 2 миллиона токенов в месяц без ввода карточки.

### Q: Нужно ли добавлять ключи для Groq и Gemini?
**A:** НЕТ! Они уже настроены и работают без тебя.

### Q: Может ли быть несколько Mistral ключей?
**A:** Да, но один даст 2М токенов/месяц, чего достаточно.

### Q: Когда ключ начинает работать?
**A:** Мгновенно! Можешь использовать сразу после создания.

### Q: Что если я ошибусь при добавлении ключа?
**A:** Система создает автоматическую резервную копию (.env.backup).

### Q: Как я узнаю что Mistral работает?
**A:** Просмотри метрики: `curl http://localhost:8000/dialogue_metrics`

### Q: А если я не добавлю Mistral?
**A:** Система работает и без него! Mistral - это просто для лучшей надежности.

### Q: Есть ли риск при добавлении?
**A:** НЕТ! Система полностью совместима, все тестируется автоматически.

---

## 🔒 БЕЗОПАСНОСТЬ

### Где хранится ключ?
- В файле `.env` локально
- Не отправляется никуда
- Используется только для API запросов к Mistral

### Что делать если ключ скомпрометирован?
1. Зайди на https://console.mistral.ai/
2. Удали скомпрометированный ключ
3. Создай новый
4. Обнови в `.env`
5. Перезапусти сервисы

### Резервная копия .env
```bash
# Резервная копия создается автоматически
ls -la /home/sv4096/rvx_backend/.env*

# Восстановить из резервной копии если нужно
cp /home/sv4096/rvx_backend/.env.backup /home/sv4096/rvx_backend/.env
```

---

## 📊 МОНИТОРИНГ

### Посмотреть использование

```bash
# Полные метрики
curl http://localhost:8000/dialogue_metrics | python3 -m json.tool

# Только Mistral статистика
curl http://localhost:8000/dialogue_metrics | python3 -c "import sys, json; d=json.load(sys.stdin); print(json.dumps(d['data']['providers']['mistral'], indent=2))"
```

### Примеры вывода

**Mistral еще не использовался:**
```json
{
  "requests": 0,
  "success": 0,
  "errors": 0,
  "timeouts": 0
}
```

**Mistral использовался (например, когда Groq был недоступен):**
```json
{
  "requests": 5,
  "success": 5,
  "errors": 0,
  "timeouts": 0
}
```

---

## 🎉 ИТОГОВАЯ СХЕМА

### После добавления Mistral система станет:

```
✅ НАДЕЖНОСТЬ:  Groq → Mistral → Gemini → Fallback
✅ СКОРОСТЬ:    400ms (Groq) → 350ms (Mistral) → 2s (Gemini)
✅ СТОИМОСТЬ:   $0 (полностью бесплатно!)
✅ ГАРАНТИЯ:    Любой диалог получит ответ
✅ ПРОСТОТА:    Нужно добавить один ключ и перезапустить
```

**БОТ СТАНЕТ ЕЩЕ НАДЕЖНЕЕ! 🚀**

---

## 📞 ПОДДЕРЖКА

Если что-то не работает:

1. **Проверить синтаксис ключа:**
   ```bash
   grep "MISTRAL_API_KEY" /home/sv4096/rvx_backend/.env
   # Должно начинаться с sk_
   ```

2. **Проверить логи:**
   ```bash
   tail -50 /tmp/api_server.log | grep -i mistral
   ```

3. **Проверить что сервисы запущены:**
   ```bash
   ps aux | grep -E "python3.*(api_server|bot)" | grep -v grep
   ```

4. **Если ошибка - восстановить резервную копию:**
   ```bash
   cp /home/sv4096/rvx_backend/.env.backup /home/sv4096/rvx_backend/.env
   bash /tmp/restart_services.sh
   ```

---

**Версия:** v0.24 Mistral Integration  
**Статус:** ✅ Production Ready  
**Дата:** 8 декабря 2025

