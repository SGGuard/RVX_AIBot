# RVX Bot v0.6.0 - Интеллектуальная адаптация и расширенная навигация

## 🎯 Основные улучшения

### 1. **Интеллектуальная адаптация контента (Knowledge-Aware Analysis)**

#### Описание:
Система теперь анализирует новости с учетом **уровня знаний пользователя** и его **прогресса в обучении**.

#### Как это работает:
- Каждый запрос теперь включает контекст пользователя:
  - `knowledge_level`: beginner, intermediate, expert
  - `course_progress`: список завершенных курсов и уроков

#### Технические детали:
- **Функция**: `build_user_context_prompt()` в `education.py`
- **Использование**: Встроена в `call_api_with_retry(user_id=...)`
- **API Integration**: Дополнительное поле `user_context` в JSON запросе к API

#### Пример адаптации:
```
Для новичка (beginner):
- Объяснение на простом языке
- Аналогии из повседневной жизни
- Избегание технических терминов

Для продвинутого (intermediate):
- Технические детали с объяснениями
- Ссылки на связанные концепции
- Примеры применения

Для эксперта (expert):
- Глубокие технические детали
- Реализация и архитектура
- Источники для дополнительного изучения
```

#### Изменения кода:
```python
# bot.py - line 2351
simplified_text, proc_time, error = await call_api_with_retry(user_text, user_id=user.id)

# education.py - новые функции
def build_user_context_prompt(cursor, user_id, base_prompt) -> str
def get_user_course_summary(cursor, user_id) -> str
```

---

### 2. **Навигация по урокам (Next Lesson Navigation)**

#### Описание:
Пользователи могут плавно переходить от одного урока к следующему с помощью кнопки **"▶️ Следующий урок"**.

#### Как это работает:
1. При просмотре урока добавляется кнопка `▶️ Следующий урок`
2. Кнопка доступна, если урок не последний в курсе
3. Клик загружает следующий урок в том же окне сообщения

#### Новые компоненты:
- **Функция**: `get_next_lesson_info(course_name, lesson_num)` в `education.py`
- **Callback handler**: `next_lesson_*` в `button_callback()` в `bot.py`
- **Функция помощника**: `split_lesson_content()` отделяет контент от quiz

#### Технические детали:
```python
# Проверка следующего урока
next_lesson_info = get_next_lesson_info(course_name, lesson_num)
if next_lesson_info:
    keyboard.append([
        InlineKeyboardButton("▶️ Следующий урок", 
                           callback_data=f"next_lesson_{course_name}_{lesson_num + 1}")
    ])
```

#### Пример потока:
```
Пользователь смотрит: Урок 1/5 "Что такое блокчейн"
                    ↓
         Нажимает "▶️ Следующий урок"
                    ↓
Загружается: Урок 2/5 "Криптография и хэши"
                    ↓
        Снова доступна кнопка "▶️ Следующий урок"
```

---

### 3. **Закладки для инструментов (Tool Bookmarks)**

#### Описание:
Пользователи могут сохранять избранные криптотехнические инструменты для быстрого доступа.

#### Команды:
- `/bookmark <инструмент>` - Добавить инструмент в закладки
- `/bookmarks` - Показать все сохраненные инструменты

#### Как это работает:
1. **Добавление**: `/bookmark Etherscan`
2. **Проверка**: Система проверяет, существует ли инструмент
3. **Сохранение**: Данные сохраняются в БД таблице `user_bookmarks`
4. **Просмотр**: `/bookmarks` показывает все закладки с кратким описанием

#### База данных:
```sql
CREATE TABLE IF NOT EXISTS user_bookmarks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,
    tool_name TEXT,
    added_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(user_id)
)
```

#### Функции:
- `bookmark_command()` - Добавить в закладки
- `bookmarks_command()` - Показать закладки

#### Пример использования:
```
Пользователь: /tools Etherscan
  ↓
Показывается информация с рекомендацией
"Хотите сохранить в избранное? /bookmark Etherscan"
  ↓
Пользователь: /bookmark Etherscan
✅ Etherscan добавлена в закладки!
  ↓
Пользователь: /bookmarks
📌 ВАШИ ЗАКЛАДКИ:
- Etherscan (explorer)
- Metamask (wallet)
- Remix IDE (development)
```

---

## 📊 Сводка изменений файлов

### `bot.py` (2820 строк)
- ✅ Добавлен `user_quiz_state` словарь для отслеживания состояния quiz (строка 824)
- ✅ Обновлена функция `call_api_with_retry()` для включения `user_context` (строки 890-975)
- ✅ Обновлена `lesson_command()` с кнопкой "Следующий урок" (строки 1480-1495)
- ✅ Добавлены команды `bookmark_command()` и `bookmarks_command()` (строки 1670-1795)
- ✅ Добавлен callback handler `next_lesson_*` (строки 2218-2290)
- ✅ Зарегистрированы новые команды (строки 2772-2773)

### `education.py` (757 строк)
- ✅ Добавлены логи (строка 9)
- ✅ Добавлена функция `split_lesson_content()` (строки 211-228)
- ✅ Добавлена функция `get_next_lesson_info()` (строки 633-658)
- ✅ Добавлена функция `build_user_context_prompt()` (строки 661-726)
- ✅ Добавлена функция `get_user_course_summary()` (строки 729-757)

---

## 🔄 Поток пользователя v0.6.0

### Сценарий 1: Адаптивный анализ новости
```
1. Пользователь отправляет новость о крипто
2. Система определяет его knowledge_level из БД
3. Запрашивает API с контекстом: "Пользователь intermediate, завершил Blockchain"
4. API отвечает адаптированным объяснением
5. Пользователь видит ответ в подходящем для него стиле
```

### Сценарий 2: Навигация по курсу
```
1. Пользователь запускает курс: /start_blockchain_basics
2. Смотрит урок 1: /lesson 1
3. Видит кнопку "▶️ Следующий урок"
4. Нажимает кнопку → Загружается урок 2 в том же сообщении
5. На последнем уроке кнопка исчезает
```

### Сценарий 3: Сохранение инструментов
```
1. Пользователь изучает инструменты: /tools
2. Находит полезный инструмент: /tools Etherscan
3. Сохраняет: /bookmark Etherscan
4. Позже быстро просмотрит: /bookmarks
5. Может быстро перейти к интересующему инструменту
```

---

## ✅ Проверенные компоненты

- ✓ Синтаксис кода - без ошибок
- ✓ Импорты обновлены
- ✓ База данных - таблица `user_bookmarks` существует
- ✓ API контекст - поле `user_context` добавлено в payload
- ✓ Навигация по урокам - callback handler зарегистрирован
- ✓ Команды закладок - зарегистрированы в приложении

---

## 🚀 Как использовать

### Новые команды:
```bash
/bookmark Etherscan      # Добавить инструмент в закладки
/bookmarks               # Показать все закладки
/lesson 1                # Просмотреть урок 1 (с кнопкой "Следующий")
▶️ Нажать кнопку        # Перейти к следующему уроку
```

### API интеграция:
Теперь все анализы включают контекст пользователя:
```json
{
  "text_content": "Bitcoin ETF одобрен...",
  "user_context": {
    "knowledge_level": "intermediate",
    "course_progress": "Blockchain: 2/5"
  }
}
```

---

## 📈 Следующие шаги (v0.7.0)

1. **Quiz система** - Интерактивные тесты после каждого урока
2. **Прогресс трекинг** - Визуальное представление прогресса (прогресс-бары)
3. **Достижения** - Значки и награды за милстоуны
4. **Персонализированные рекомендации** - На основе прогресса и интересов
5. **Социальный лидерборд** - Соревнования между пользователями

---

**Версия**: v0.6.0  
**Дата**: 29 ноября 2025 г.  
**Статус**: ✅ Готово к использованию  
**Тестирование**: Пройдено
