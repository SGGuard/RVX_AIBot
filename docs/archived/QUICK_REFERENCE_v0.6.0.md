# RVX Bot v0.6.0 - Краткий справочник

## 🎮 Новые команды

### Закладки инструментов
```
/bookmark Etherscan     Добавить инструмент в закладки
/bookmarks              Показать все сохраненные инструменты
```

### Просмотр уроков
```
/lesson 1               Просмотреть урок 1 (с кнопкой "Следующий урок")
/start_blockchain_basics  Начать курс Blockchain Basics
```

## 🔘 Новые кнопки

### На экране урока
- **🎯 Начать тест** - Пройти тест по уроку (v0.7.0)
- **▶️ Следующий урок** - Перейти к следующему уроку

### На экране инструмента
- Текст: "Хотите сохранить в избранное? /bookmark Etherscan"

## 📊 Адаптивные ответы

### Зависит от knowledge_level пользователя:

**beginner (новичок)**
- Простой язык без терминов
- Аналогии из жизни
- Базовые концепции

**intermediate (средний)**
- Технические детали
- Примеры использования
- Связанные концепции

**expert (эксперт)**
- Глубокий анализ
- Реализация и архитектура
- Источники и ссылки

## 🗄️ База данных

Новая таблица: `user_bookmarks`
```sql
CREATE TABLE user_bookmarks (
    id INTEGER PRIMARY KEY,
    user_id INTEGER,
    tool_name TEXT,
    added_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

## 🔧 Как это работает

### 1. Адаптивный анализ

```python
# Бот автоматически:
1. Получает knowledge_level пользователя из БД
2. Получает прогресс по курсам
3. Добавляет контекст в API запрос
4. API возвращает адаптированный ответ

# API теперь получает:
{
  "text_content": "новость...",
  "user_context": {
    "knowledge_level": "intermediate",
    "course_progress": "Blockchain: 2/5"
  }
}
```

### 2. Навигация по урокам

```python
# При просмотре урока:
1. Проверяется наличие следующего урока
2. Если существует, добавляется кнопка
3. При клике - загружается следующий урок
4. Пользователь получает 5 XP за просмотр
```

### 3. Закладки инструментов

```python
# При добавлении в закладки:
1. Проверяется существование инструмента
2. Проверяется, не добавлен ли уже
3. Сохраняется в БД (user_bookmarks)
4. Пользователь может просмотреть /bookmarks

# При просмотре закладок:
1. Получаются все инструменты пользователя
2. Выводятся с кратким описанием
3. Возможен быстрый переход к полной информации
```

## 📈 Интеграция с API

### Старый формат:
```json
{
  "text_content": "Bitcoin ETF одобрен..."
}
```

### Новый формат v0.6.0:
```json
{
  "text_content": "Bitcoin ETF одобрен...",
  "user_context": {
    "knowledge_level": "intermediate",
    "course_progress": "Blockchain: 2/5 (40%)\nDeFi: 1/5 (20%)"
  }
}
```

## 🚀 Тестирование

### Тест 1: Адаптивный анализ
```bash
# Отправить новость боту
# Проверить, что ответ соответствует уровню пользователя

# Ожидается:
# - Для beginner: простые объяснения
# - Для intermediate: технические детали
# - Для expert: глубокий анализ
```

### Тест 2: Навигация по урокам
```bash
# 1. /start_blockchain_basics
# 2. /lesson 1
# 3. Проверить наличие кнопки "▶️ Следующий урок"
# 4. Нажать кнопку
# 5. Проверить загрузку урока 2
# 6. Повторить для всех 5 уроков
```

### Тест 3: Закладки
```bash
# 1. /tools
# 2. /tools Etherscan
# 3. /bookmark Etherscan
# 4. Проверить "✅ Etherscan добавлена в закладки!"
# 5. /bookmarks
# 6. Проверить Etherscan в списке
```

## 📝 Логирование

Все события логируются:
```
📚 Добавлен контекст пользователя X: уровень=intermediate
⭐ Пользователь X получил 5 XP за урок 1
✅ Etherscan добавлена в закладки пользователем X
▶️ Пользователь X нажал "Следующий урок"
```

## 🔄 Обновленные функции

| Функция | Файл | Изменение |
|---------|------|-----------|
| `call_api_with_retry()` | bot.py | Теперь принимает user_id и добавляет контекст |
| `lesson_command()` | bot.py | Добавлена кнопка "Следующий урок" |
| `button_callback()` | bot.py | Добавлен обработчик next_lesson_* |

## 📦 Новые функции

| Функция | Файл | Назначение |
|---------|------|-----------|
| `build_user_context_prompt()` | education.py | Построение контекста пользователя |
| `get_user_course_summary()` | education.py | Резюме прогресса по курсам |
| `get_next_lesson_info()` | education.py | Информация о следующем уроке |
| `split_lesson_content()` | education.py | Разделение урока и quiz |
| `bookmark_command()` | bot.py | Добавить в закладки |
| `bookmarks_command()` | bot.py | Показать закладки |

## 🐛 Если что-то не работает

### Бот не запускается
```bash
# Проверить синтаксис
python3 -m py_compile bot.py

# Проверить импорты
python3 -c "from education import *"

# Запустить с логами
python3 bot.py 2>&1 | head -100
```

### Команды не работают
```bash
# Проверить регистрацию команд
grep "CommandHandler.*bookmark" bot.py

# Проверить в логах
grep "bookmark" bot.log
```

### БД проблемы
```bash
# Проверить таблицу
sqlite3 rvx_bot.db "SELECT * FROM user_bookmarks LIMIT 5;"

# Пересоздать БД если нужно
rm rvx_bot.db
python3 bot.py  # Пересоздаст при запуске
```

## 💡 Советы по использованию

1. **Для новичков**: Система автоматически даст простые объяснения
2. **Для экспертов**: Получайте глубокий анализ с источниками
3. **Навигация**: Используйте кнопки для плавного перемещения по урокам
4. **Закладки**: Сохраняйте нужные инструменты для быстрого доступа

## 📞 Версионирование

- **v0.5.0**: Система обучения (курсы, уроки, XP)
- **v0.6.0**: Адаптация + Навигация + Закладки
- **v0.7.0** (планируется): Интерактивные quiz, достижения

---

**Последнее обновление**: 29 ноября 2025 г.  
**Версия**: v0.6.0  
**Статус**: ✅ Активна
